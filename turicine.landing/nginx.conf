server {
    listen 8080;
    server_name _;

    # Solo sirve el SPA. /api hacia el backend lo enruta el proxy principal (externo).
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
