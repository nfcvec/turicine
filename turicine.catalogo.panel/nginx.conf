server {
    listen 8080;
    server_name _;

    # Solo sirve el SPA. El enrutamiento de /api y /odata hacia el backend lo hace
    # el proxy principal (externo), no este contenedor.
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
